// Production pages import only this registry. Pending review bundles must never be
// imported here; publication eligibility is also rechecked against source status.
export const productionEditorialIssueSlices = Object.freeze([]);
