# Foushee Justice Semantic IR M5-R1

## Milestone intent

Correct the reviewed same-role primary overlap in the detached Foushee Justice
Semantic IR without changing the canonical compiler, accepted corpora, or the
byte-preserved M5 V1 evidence root.

## Scope

- Build complete action and episode overlap ledgers for V1 and corrected V2.
- Remove the executable `pretrial_release_regulation` trait because its only two
  episodes cannot form an independent repeated pattern once the D.C. episode is
  counted in the primary D.C. rule-displacement pattern.
- Preserve roll 298 in the D.C. pattern and compile roll 171 as a bounded
  notable choice.
- Recompile through the canonical pipeline; reevaluate synthesis and conclusion
  ownership; regenerate the provisional implementation, risk successor,
  calibration population, dossier, schemas, parity, and review package.
- Add independent overlap validation and adversarial regression tests.

## Non-scope

- No changes to M5 V1, prior governed roots, canonical compiler behavior,
  accepted Semantic IR corpora, runtime, persistence, public UI, publication,
  production, deployment, network, or database state.
- No push, pull request, merge, or launch-sample selection.

## Definition of done

1. Every action and episode is mapped to its behavioral propositions.
2. No prohibited same-role primary overlap remains.
3. Roll 298 supplies primary pattern weight once; roll 171 remains explicitly
   accounted for.
4. The corrected graph and implementation deterministically reconstruct from
   the V2 compiler input using the canonical compiler and pipeline.
5. All 37 actions remain accounted for and special-roll boundaries remain
   unchanged.
6. Synthesis alternatives, risk continuity, calibration eligibility, parity,
   and isolation are explicitly verified.
7. One local unpushed commit and one independently verified external review ZIP
   are produced.

## Validation

Run the complete prior authority chain, canonical Semantic IR validation,
M5 V1 checks, M5-R1 builder and independent verifier, adversarial unit tests,
the broadest safe offline backend suite, Ruff, Python compilation, JSON/schema
validation, credential scan, and `git diff --check`.
