# Milestone Plan: IR-native Foushee Justice presentation

## Intent

- Immediate task: add a publication-gated IR-native conclusion layer to the
  coverage-first representative route for F000477 / Justice & Public Safety /
  119th Congress.
- Larger-goal alignment: explain reviewed legislative behavior without
  recreating deleted editorial architecture or allowing React to infer meaning.

## Outcome

- The backend compiles stable Semantic IR identities and separately reviewed
  wording into a deterministic artifact and fail-closed public payload.
- Existing issue cards and receipts can display an eligible supplied tier.
- With no authorized approval receipt, the real fixture and API remain
  unpublished and `receipts_only`.

## Scope And Boundaries

- In scope: contract/schema, compiler/validator, read-only selector/API, pending
  Foushee review fixture, display-only React, receipt navigation, tests,
  rendered validation, and directly affected documentation.
- Out of scope: migration, production/database access, persistence, registry
  mutation, deployment, merge, Economy/Environment artifacts, counting or
  methodology changes, and cross-time claims.

## Decision Envelope

- Codex may decide deterministic structure, serializer shape, generic tier
  rendering, test fixtures, and responsive layout within the supplied contract.
- Human approval is required for editorial approval, benchmark promotion,
  production eligibility, publication activation, deployment, and merge.

## Definition Of Done

- [x] Compiler and validator preserve compiled meaning and gates.
- [x] API fails closed and preserves 119/all/118 scope behavior.
- [x] Display-only frontend preserves card order and existing receipts.
- [x] Supporting-vote controls resolve canonical action IDs.
- [x] Full requested validation and rendered inspection completed.
- [x] Review documentation reconciled.
- [ ] One cohesive draft PR opened.

## Baseline

- Branch/base:
  `codex/ir-native-foushee-justice-presentation` from
  `8ce6c1d7efe50075c4118877c8cf572b6561516d`.
- Production/deployment: not queried or changed.
- Starting worktree: verified clean on `main`.
- Unrelated artifacts: none present at baseline.

## Implementation Sequence

1. Verify baseline and read the semantic, cutover, frontend, and publication
   contracts.
2. Add contract, compiler/validator, pending fixture, selector, and API.
3. Add display-only integration and receipt navigation.
4. Add positive, negative, invariance, accessibility, and responsive tests.
5. Run the requested validation matrix, inspect outputs/renders/diff, correct
   defects, and open a draft PR.

## Progress Checklist

- [x] Discovery
- [x] Backend implementation
- [x] Frontend implementation
- [x] Focused backend and frontend unit validation
- [x] Browser and release validation
- [x] Documentation reconciliation
- [x] Commit/PR readiness

## Discoveries

- The exact requested baseline was clean and matched.
- No authorized human receipt exists; historical Justice approval fields remain
  pending.
- The immutable-artifact repository already owns the publication-selector seam.

## Decisions And Rationale

- The real authoring fixture retains candidate wording, but its compiler-owned
  public display is `receipts_only` until every independent gate passes.
- Tiers derive from compiled proposition shape and typed boundaries, not totals.
- `scope=all` may carry a 119th read only with an explicit boundary;
  `scope=118` cannot.
- React receives supplied fields and never derives a tier or conclusion.

## Deviations Or Corrections

- Corrected a test helper that aliased compiled action-ID arrays; production
  compiler behavior was unaffected.
- Removed a coverage-derived card label that conflicted with the API-supplied
  tier; evidence counts and card ordering remain unchanged.
- Updated five source-contract assertions after the issue introduction began
  naming the publication gate explicitly.
- The first sandboxed release validation could not spawn the frontend build on
  Windows (`EPERM`); the identical command passed outside that sandbox.

## Validation Results

- Semantic validation: 5 checks passed.
- Requested Justice domain cases: 3 passed.
- Requested backend presentation/API/cutover suite: 31 passed.
- Frontend unit suite: 97 passed.
- Frontend production build: passed with existing non-failing hook warnings.
- Cutover browser suite: 3 passed.
- IR presentation browser suite: 3 passed, including keyboard access,
  390-pixel responsive rendering, receipt navigation, and the golden fixture
  remaining 404.
- Release validation with frontend: 6 checks passed.
- Documentation governance and `git diff --check`: passed.
- Desktop and mobile visual inspection preserved the evidence path and found no
  horizontal overflow.

## Production Writes

- Performed: no
- Scope: none
- Expected effects: none
- Actual effects: none

## Rollback Paths

- Revert the branch/PR. No migration, persistence, registry, database,
  production, or deployment state exists to roll back.

## Blockers

- Public activation requires an authorized receipt approving the conclusion,
  both repeated patterns, fentanyl limitation, claim/source mappings, benchmark
  promotion, and production eligibility.

## Final Reconciliation

- Definition of done satisfied for implementation, validation, and review
  preparation; draft PR creation is the remaining repository action.
- Remaining limitation: approval gate intentionally unresolved.
- Recommended next step: route the bounded fixture for authorized review
  without activating publication.
