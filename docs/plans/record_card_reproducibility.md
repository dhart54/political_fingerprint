# Record-card reproducibility (#192)

## Intent and outcome
Build one reusable candidate transformation of existing publication-gated issue
presentations. Ordinary content changes flow through the existing read API, without
member code, selection lists, rewritten card copy, storage, or scheduling.

## Baseline and boundaries
Base: `2049eaf25e2ab9f197dafe6ad97d9f8e0cc3c257`; isolated branch
`codex/reproducible-record-cards`. The root workspace contains unrelated work.
The released V1 content, publication artifacts, catalogs and authority stay intact.
No production writes, merge, deployment, publication or new operating authority.
Read interpretation principles and the editorial-standardization workflow before
implementation. Typed findings own meaning; this transformation only selects and
renders their existing text, exact action sets, relationships and boundaries.

## Decision envelope and definition of done
Candidate rules and forms may be proposed and implemented for consolidated review.
No new legislative interpretation or member approval may be fabricated. Deliver
working reference/update/held-aside proofs, a real eligible second member or an
exact upstream dependency, focused regressions, representative rendered checks,
and one draft PR with exact-head checks. Expect roughly 10–15 scoped files and
frontend release validation; no domain regeneration or database work.

## Sequence and progress
- [x] Read issue/specification and isolate baseline.
- [x] Trace inputs and document/fix policy before final held-aside replay.
- [x] Implement pure shared projection and opt-in existing review journey.
- [x] Run unchanged/update/held-aside/unsupported proofs; identify second-member dependency.
- [x] Run focused units, browser checks and build.
- [ ] Exact-head CI and one draft PR.

## Proof design
Reference: verified post-M15B Foushee 119 public response. Initial hold-aside was
the actual pre-activation `before-live.json.gz` response. After rendered form
corrections, reserve the previously unevaluated `after-ns-live` all-scope response
for the final fixed-rule evaluation. Replay pre/post activation with identical rules.
The archived activation manifests establish provenance; a simulated edit will
not be represented as an authorized upstream revision.
Second member discovery: shared corpus contains Grothman's official member
projection and compiled-proposition parity proof. Public finding eligibility must
be established separately. Historical pre-IR member prose cannot be adapted.

## Discoveries and decisions
The existing read interface already separates publication authority from optional
card validity. A pure data-driven projection can consume it without another API,
registry, frontend content bundle or backend configuration. Candidate adoption
remains opt-in and local; existing public V1 stays the default.

## Validation / deviations / blockers
Rendered review corrected unscoped issue limitations being attached to unrelated
entries and added source titles to component qualifications. This changed reading
cost and therefore the output (seven to five entries) without changing the budget
or tie-break to recover preferred entries. Missing contextual siblings now keep
all remaining children inside the failed bundle. No accepted source was changed.

## Production writes and rollback
None. Code-only branch changes can be discarded/reverted; no production rollback
is involved. Add this branch to the existing Vercel no-deployment list before push.

## Final reconciliation
Implementation, actual-source replay and representative rendered validation are
complete. 177 frontend units, 118 backend regressions, 51 browser checks passed;
one optional existing capture skipped. Build passed with eight existing hook
warnings. Both generators pass --check; no V1 source artifacts changed.
Second-member reuse remains blocked by eligible reviewed IR-native findings and
display wording. Grothman's frozen shared-corpus replay matched its existing
member digest, but is not a reviewed presentation. The historical update refreshed
bindings and removed an omitted finding; it did not change selected visible copy.
Full results, intervention counts and screenshots are in the compact review packet.
Draft PR/exact-head hosted check result will be recorded in the PR discussion.
